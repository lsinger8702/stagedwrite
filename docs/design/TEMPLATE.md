# NNN：功能名称

**先核对 [000 项目原则](000-project-principles.md)。原则冲突必须先与项目所有者讨论并取得明确同意；未决项不能写成既定方案。**

## 原则对齐与决策状态

引用适用的原则编号；分开列出已确认语义、未决接口、当前实现及待对齐差异。如需改变原则，记录具体提案、用户决定及日期；没有决定不得开始依赖该选择的实现。


状态：草案 / 已确认 / 已实现。关联任务：M?。

## 目的与范围

这次解决什么；不解决什么；依赖哪些已完成能力。

## 使用示例

给出完整输入与可观察输出。标明是拟议 API 还是现有 API。

## 数据结构

身份、版本、存储字段、快照/可变性，以及与其他模块的边界。

## 行为规则

处理顺序、不变量、事务边界、并发约束。

## 失败处理

错误码、是否改变状态、调用者下一步能做什么。

## 验收案例

正常路径、非法输入、失败后的状态；每项应能对应测试或明确的手工验收。

## 未决项

必须先解决的项与可以延后的项分别列出。

## 实现记录

完成后填写实际文件、验证结果和与原设计的差异。
