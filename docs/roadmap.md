# 开发任务与进度

日期：2026-09-12。这里是本地任务清单，还没有同步为 GitHub Issues。

## 已完成：探索原型

- [x] 单包 TypeScript、README、许可证、CI 配置。
- [x] 顶层字段 set/remove/reset 与版本检查。
- [x] 同步规则、修复建议、预检凭据。
- [x] 内存执行、UNKNOWN 查证恢复和模拟远端。
- [x] 10 项原型测试。

## 核心 MVP：按顺序推进

| 编号 | 功能 | 依赖 | 状态 | 完成标志 |
|---|---|---|---|---|
| M1 | 定义装配与空图创建 | 无 | 待实现，已有设计草案 | 装配、本地 schema 引用、错误拒绝、版本绑定示例通过 |
| M2 | 图 OP 与结构校验 | M1 | 待设计 | 原子批次、关系完整性、类型错误测试通过 |
| M3 | 图上的预检与修复 | M2 | 待设计 | 缺项诊断、修复、旧凭据失效闭环 |
| M4 | 草稿与预检持久化 | M3 | 待设计 | 重启后图、版本和检查记录可读 |
| M5 | 固定计划与执行记录持久化 | M4 | 待设计 | 多步执行保留部分成功与原请求 |
| M6 | UNKNOWN 与重启恢复 | M5 | 待设计 | 跨进程故障注入通过 |
| M7 | 外部试用与 0.1.0 发布 | M6 | 待完成 | 干净环境跑通、README 与实现一致 |

后续：Stripe 测试接入 → MCP 外壳。二者不算核心 MVP 的已完成能力。

## 当前下一步：M1

读 design/001-registry-and-draft.md，明确未决项，完成最小注册与 create。保持已有示例可运行；若接口变化，同一提交内更新例子和测试。

建议拆成三个可检查的提交：

1. 定义类型和注册表：独立定义、启动原子装配与冻结、版本索引。
2. 创建绑定定义的空图：稳定 ID、版本、只读快照。
3. 注册示例和验收测试：补 README，标记 M1 完成。

## 每个任务怎么推进

1. 从 design/TEMPLATE.md 建一份短设计。
2. 写清输入输出、错误行为、验收案例，不追求大量类图。
3. 实现一个完整行为，同时写针对其失败风险的测试。
4. 跑相关测试和示例；更新 roadmap 和 README。
5. 提交一份可运行版本。需要外部讨论时，再开小 PR。

完成标准：代码实现、验收通过、可运行示例、状态文档四项齐全。仅有设计或空接口不能打勾。

## 复杂场景约束（2026-09-12）

M1 草案已更新为 defineDraftType → createStagedWrite → create。M2 设计前阅读 [适配矩阵](design/002-complex-draft-fit.md)：确定纯 evaluateEdit、身份不复用、父子路径覆盖和意图一致性。

未来复杂场景扩展（未实现，不与核心 MVP 混为一谈）：嵌套 schema/OP、命名空间、初始图导入、基线 restore、revision/run、只读证据与有副作用获取分离。不得因为有注册接口就标记这些能力完成。

M1 引用范围已补充：valueSchema 内 $defs / $ref；装配时解析、拒绝悬空与循环、摘要覆盖引用目标。图节点 ref 始终保留在图模型设计中，具体操作属 M2。本次只更新设计，未实现上述能力。
