# Copy to .env.stripe (ignored by git); fill locally. Never commit real keys.
STRIPE_SECRET_KEY=
# Dashboard sandbox account ID. Must belong to the key above, not your live account.
STRIPE_SANDBOX_ACCOUNT=
